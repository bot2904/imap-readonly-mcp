{
  "id": "0614f3e0",
  "title": "Remove documented-but-unimplemented or unnecessary non-IMAP functionality",
  "tags": [
    "cleanup",
    "docs",
    "pi"
  ],
  "status": "closed",
  "created_at": "2026-06-09T23:49:09.744Z"
}

Completed IMAP-only cleanup.

Removed/cleaned:
- Removed POP3 and Microsoft Graph connectors and tests.
- Simplified configuration to accept only `protocol: imap`; removed OAuth, Google/Gmail, Microsoft Graph, rate-limit, and protocol-specific non-IMAP config fields.
- Simplified connector registry and service connector selection to IMAP only.
- Removed POP3-specific folder policy behavior.
- Updated README, architecture docs, example config, project metadata, tool descriptions, and model descriptions to describe IMAP-only behavior.
- Removed optional Graph/Google dependency extras and refreshed `uv.lock`.
- Deleted stale `.clawpatch` audit artifacts that documented removed POP3/Graph issues.

Verification:
- `UV_CACHE_DIR=.uv-cache uv lock`
- `UV_CACHE_DIR=.uv-cache uv run --extra dev python -m pytest` (22 passed)
- `UV_CACHE_DIR=.uv-cache uv run --extra dev ruff check .` (passed)
- Searched source/docs/config/tests/project metadata for POP3, Graph, Gmail, Google, OAuth, Microsoft, provider references; remaining non-product hits are only false positives in dependency/license text or todo history.
