{
  "id": "4f810a52",
  "title": "Add docker-run-account option to pass IMAP password as parameter",
  "tags": [
    "docker",
    "scripts"
  ],
  "status": "open",
  "created_at": "2026-06-16T23:54:25.369Z"
}

Add an option to `scripts/docker-run-account.sh` that lets callers provide the selected account's IMAP password via a command-line parameter instead of requiring it in the env file/env var.

Considerations:
- Preserve current env-file behavior for compatibility.
- Avoid leaking the password in dry-run output, logs, or process listings if possible.
- Decide option shape, e.g. `--password VALUE`, `--password-file FILE`, or both.
- Map the provided value to `MAIL_ACCOUNT__PASSWORD` after account selection.
- Add/update documentation and tests if applicable.
