{
  "id": "e68b9633",
  "title": "Audit read-only guarantees and mailbox filtering logic",
  "tags": [
    "audit",
    "security",
    "filtering"
  ],
  "status": "open",
  "created_at": "2026-05-29T02:13:56.958Z"
}

Verify that the codebase is strictly read-only and investigate the mechanics of mailbox filtering.

### Verification Tasks
- [ ] Audit all connectors (`imap`, `pop3`, `graph`) for mutating operations (e.g., `STORE`, `DELETE`, `POST/PATCH/DELETE` requests).
- [ ] Confirm `BODY.PEEK` is used consistently in all IMAP fetch paths.
- [ ] Check if any middleware or service layer components perform write operations (e.g., cache pruning, status updates).

### Filtering Investigation
- [ ] Review how `allowed_folders` and `excluded_folders` are applied in `MailService`.
- [ ] Check if filtering happens at the connector level (search criteria) or service level (post-processing).
- [ ] Determine if sub-folders are automatically included/excluded based on parent paths.
