# clawpatch report

findings: 1

## medium: mail_fetch ids parameter is required despite being documented as optional

id: fnd_sig-feat-cli-command-4e6896962a-_0b9a3d01db
category: api-contract
confidence: medium
triage: contract-mismatch
status: open
feature: Python CLI command imap-readonly-mcp (feat_cli-command_4e6896962a)

evidence:
- src/imap_readonly_mcp/server.py:443-450 (mail_fetch)
- src/imap_readonly_mcp/server.py:510-522 (mail_fetch)

The `ids` parameter has no Python default value, while its Field metadata uses `default_factory=list` and the tool description says callers can leave it empty. If FastMCP invokes the function without supplying `ids`, Python treats it as a required argument and can raise a missing-argument error before the normalization at lines 510-522 can run. The neighboring parameters all have explicit defaults, making this likely accidental.

recommendation:
Give `ids` an explicit function default, e.g. `ids: Annotated[list[str], Field(...)] = []` or preferably `ids: Annotated[list[str] | None, Field(default=None, ...)] = None`, then normalize to an empty list/None internally.

test analysis:
No tests are included for this feature, and the excerpt does not show a tool-schema or direct invocation test that omits `ids`.

suggested regression test:
Add a test that invokes `mail_fetch` through the MCP/FastMCP interface with only default arguments or only `query`, asserting it does not fail due to a missing `ids` argument.

minimum fix scope:
Update the `mail_fetch` function signature and any corresponding input normalization/types.

